/**
 * Topology. Identical in dev and prod — Nas's reverse proxy resolves these
 * names in both. Not configurable, deliberately.
 */
export const DWAR_BASE_URL = "http://dwar.dadi";
export const HOST = "0.0.0.0";
export const PORT = 8080;
