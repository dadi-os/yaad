# Postgres. Nas reads POSTGRES_PASSWORD for the database container and
# DATABASE_URL for the service; keep the password consistent between them.
POSTGRES_PASSWORD=yaad
DATABASE_URL=postgres://yaad:yaad@yaad-postgres:5432/yaad
